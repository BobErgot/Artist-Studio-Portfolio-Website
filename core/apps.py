from django.apps import AppConfig


class CoreConfig(AppConfig):
    """
    Configuration for the 'core' application.

    This class sets the name attribute for the 'core' app, which is used by Django
    to identify the application.
    """
    name = 'core'
