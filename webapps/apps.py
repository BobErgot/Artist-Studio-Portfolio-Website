from django.apps import AppConfig


class WebappsConfig(AppConfig):
    """
    Configuration for the 'webapps' application.

    This class sets the name attribute for the 'webapps' app, which is used by Django
    to identify the application.
    """
    name = 'webapps'
